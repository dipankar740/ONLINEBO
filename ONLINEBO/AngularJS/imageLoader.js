function fImageURL(input) {
    if (input.files && input.files[0]) {
        var reader = new FileReader();

        reader.onload = function (e) {
            $('#fImage').attr('src', e.target.result).width().height();
        }; reader.readAsDataURL(input.files[0]);
    }
}

function fFontNidURL(input) {
    if (input.files && input.files[0]) {
        var reader = new FileReader();

        reader.onload = function (e) {
            $('#fFontNid').attr('src', e.target.result).width().height();
        }; reader.readAsDataURL(input.files[0]);
    }
}

function fBackNidURL(input) {
    if (input.files && input.files[0]) {
        var reader = new FileReader();

        reader.onload = function (e) {
            $('#fBackNid').attr('src', e.target.result).width().height();
        }; reader.readAsDataURL(input.files[0]);
    }
}

function fSingURL(input) {
    if (input.files && input.files[0]) {
        var reader = new FileReader();

        reader.onload = function (e) {
            $('#fSing').attr('src', e.target.result).width().height();
        }; reader.readAsDataURL(input.files[0]);
    }
}


///////////  Joint 

function jImageURL(input) {
    if (input.files && input.files[0]) {
        var reader = new FileReader();

        reader.onload = function (e) {
            $('#jImage').attr('src', e.target.result).width().height();
        }; reader.readAsDataURL(input.files[0]);
    }
}

function jFontNidURL(input) {
    if (input.files && input.files[0]) {
        var reader = new FileReader();

        reader.onload = function (e) {
            $('#jFontNid').attr('src', e.target.result).width().height();
        }; reader.readAsDataURL(input.files[0]);
    }
}

function jBackNidURL(input) {
    if (input.files && input.files[0]) {
        var reader = new FileReader();

        reader.onload = function (e) {
            $('#jBackNid').attr('src', e.target.result).width().height();
        }; reader.readAsDataURL(input.files[0]);
    }
}

function jSingURL(input) {
    if (input.files && input.files[0]) {
        var reader = new FileReader();

        reader.onload = function (e) {
            $('#jSing').attr('src', e.target.result).width().height();
        }; reader.readAsDataURL(input.files[0]);
    }
}




///////////  Authorize 

function aImageURL(input) {
    if (input.files && input.files[0]) {
        var reader = new FileReader();

        reader.onload = function (e) {
            $('#aImage').attr('src', e.target.result).width().height();
        }; reader.readAsDataURL(input.files[0]);
    }
}

function aFontNidURL(input) {
    if (input.files && input.files[0]) {
        var reader = new FileReader();

        reader.onload = function (e) {
            $('#aFontNid').attr('src', e.target.result).width().height();
        }; reader.readAsDataURL(input.files[0]);
    }
}

function aBackNidURL(input) {
    if (input.files && input.files[0]) {
        var reader = new FileReader();

        reader.onload = function (e) {
            $('#aBackNid').attr('src', e.target.result).width().height();
        }; reader.readAsDataURL(input.files[0]);
    }
}

function aSingURL(input) {
    if (input.files && input.files[0]) {
        var reader = new FileReader();

        reader.onload = function (e) {
            $('#aSing').attr('src', e.target.result).width().height();
        }; reader.readAsDataURL(input.files[0]);
    }
}


///////////  Nominee 1 

function n1ImageURL(input) {
    if (input.files && input.files[0]) {
        var reader = new FileReader();

        reader.onload = function (e) {
            $('#n1Image').attr('src', e.target.result).width().height();
        }; reader.readAsDataURL(input.files[0]);
    }
}

function n1FontNidURL(input) {
    if (input.files && input.files[0]) {
        var reader = new FileReader();

        reader.onload = function (e) {
            $('#n1FontNid').attr('src', e.target.result).width().height();
        }; reader.readAsDataURL(input.files[0]);
    }
}

function n1BackNidURL(input) {
    if (input.files && input.files[0]) {
        var reader = new FileReader();

        reader.onload = function (e) {
            $('#n1BackNid').attr('src', e.target.result).width().height();
        }; reader.readAsDataURL(input.files[0]);
    }
}

function n1SingURL(input) {
    if (input.files && input.files[0]) {
        var reader = new FileReader();

        reader.onload = function (e) {
            $('#n1Sing').attr('src', e.target.result).width().height();
        }; reader.readAsDataURL(input.files[0]);
    }
}

///////////  Nominee 2 

function n2ImageURL(input) {
    if (input.files && input.files[0]) {
        var reader = new FileReader();

        reader.onload = function (e) {
            $('#n2Image').attr('src', e.target.result).width().height();
        }; reader.readAsDataURL(input.files[0]);
    }
}

function n2FontNidURL(input) {
    if (input.files && input.files[0]) {
        var reader = new FileReader();

        reader.onload = function (e) {
            $('#n2FontNid').attr('src', e.target.result).width().height();
        }; reader.readAsDataURL(input.files[0]);
    }
}

function n2BackNidURL(input) {
    if (input.files && input.files[0]) {
        var reader = new FileReader();

        reader.onload = function (e) {
            $('#n2BackNid').attr('src', e.target.result).width().height();
        }; reader.readAsDataURL(input.files[0]);
    }
}

function n2SingURL(input) {
    if (input.files && input.files[0]) {
        var reader = new FileReader();

        reader.onload = function (e) {
            $('#n2Sing').attr('src', e.target.result).width().height();
        }; reader.readAsDataURL(input.files[0]);
    }
}


///////////  Guardian  1 

function g1ImageURL(input) {
    if (input.files && input.files[0]) {
        var reader = new FileReader();

        reader.onload = function (e) {
            $('#g1Image').attr('src', e.target.result).width().height();
        }; reader.readAsDataURL(input.files[0]);
    }
}

function g1FontNidURL(input) {
    if (input.files && input.files[0]) {
        var reader = new FileReader();

        reader.onload = function (e) {
            $('#g1FontNid').attr('src', e.target.result).width().height();
        }; reader.readAsDataURL(input.files[0]);
    }
}

function g1BackNidURL(input) {
    if (input.files && input.files[0]) {
        var reader = new FileReader();

        reader.onload = function (e) {
            $('#g1BackNid').attr('src', e.target.result).width().height();
        }; reader.readAsDataURL(input.files[0]);
    }
}

function g1SingURL(input) {
    if (input.files && input.files[0]) {
        var reader = new FileReader();

        reader.onload = function (e) {
            $('#g1Sing').attr('src', e.target.result).width().height();
        }; reader.readAsDataURL(input.files[0]);
    }
}


///////////  Guardian  2

function g2ImageURL(input) {
    if (input.files && input.files[0]) {
        var reader = new FileReader();

        reader.onload = function (e) {
            $('#g2Image').attr('src', e.target.result).width().height();
        }; reader.readAsDataURL(input.files[0]);
    }
}

function g2FontNidURL(input) {
    if (input.files && input.files[0]) {
        var reader = new FileReader();

        reader.onload = function (e) {
            $('#g2FontNid').attr('src', e.target.result).width().height();
        }; reader.readAsDataURL(input.files[0]);
    }
}

function g2BackNidURL(input) {
    if (input.files && input.files[0]) {
        var reader = new FileReader();

        reader.onload = function (e) {
            $('#g2BackNid').attr('src', e.target.result).width().height();
        }; reader.readAsDataURL(input.files[0]);
    }
}

function g2SingURL(input) {
    if (input.files && input.files[0]) {
        var reader = new FileReader();

        reader.onload = function (e) {
            $('#g2Sing').attr('src', e.target.result).width().height();
        }; reader.readAsDataURL(input.files[0]);
    }
}
